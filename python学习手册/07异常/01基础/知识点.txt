异常：
1>可以改变程序中控制流程的事件
2>语句：
try/except:捕捉python或你引起的异常并处理,处理了异常后代码就好继续执行下去，否则(finally后)结束程序
try/finally:无论异常是否发生，都执行finally中操作，若发生异常，finally之后照样抛出
raise:手动在代码中触发异常
assert:有条件的在代码中触发异常
with/as:环境管理器
3>用户定义的异常：定义继承内置异常类(Exception)的类