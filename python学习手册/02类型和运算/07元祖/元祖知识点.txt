元祖：
任意对象的有序集合
通过偏移读取
不可变序列
固定长度，异构，任意嵌套
对象引用的数组

创建元祖常量：
直接赋值：t=();t=(1,);t=(2,3,4);t=(1,(2,3))  #注意t=(1,),单一值赋值的时候一定要加逗号，否则就不是元祖了
tuple函数+可迭代对象：t=tuple('abc')

操作：
1>索引切片，t1+t2;t*3;迭代
2>t.index('ni');t.count('ni')